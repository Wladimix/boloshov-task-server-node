{
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "deploy": "npm run build && vercel"
  },
  "devDependencies": {
    "@types/co-body": "^6.1.3",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "co-body": "^6.2.0",
    "mongoose": "^8.8.4"
  }
}
